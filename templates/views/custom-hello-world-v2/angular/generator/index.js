(function (module) {
    'use strict';

    class Generator {
        // can use augment model too.

        getViewChildComponents(view) {
        }

        extendViewContext(context) {
        }
    }

    module.exports = () => {
        return new Generator();
    };
})(module);